## Code Review Fixes — 2026-05-09 (follow-up)

Based on `docs/CODE_REVIEW_2026-05-09.md`.

### C3 — CSV newline escaping (`logging.cpp`)
- Added `\n`/`\r` → space substitution in the CSV write loop
- Covers edge cases like `e.what()` messages containing newlines

### I1 — `using namespace hailort` removed from header (`hailo_infer.hpp`)
- Moved to `hailo_infer.cpp` only
- Prevents namespace pollution for all files that include the header
- Also removed unused `<iostream>` from header

### I2 — `optional<reference_wrapper<Device>>` → raw pointer (`hailo_infer.hpp`)
- Changed `device` member to `hailort::Device *device = nullptr`
- Removed `<optional>` include
- Updated `hailo_infer.cpp`: assignment uses `&physical_devices[0].get()`, access uses `this->device->get_chip_temperature()`

### I3 — `fclose` on exit (`logging.cpp`)
- Registered `atexit` lambda to call `fclose(g_csv_file)` on normal program exit
- No data loss risk (fflush per line still in place), but now cleanly closes the file descriptor

### I4 — `std::cerr` duplicates removed (`classifier.cpp`)
- Removed `std::cerr` on image read failure (line 96) — `LOG_WARN` already covers it
- Removed `std::cerr` on exception catch (line 138) — `LOG_ERROR` already covers it

### M1 — Unused `<cstring>` removed (`hailo_infer.cpp`)