# 2026-05-10 Change Log

## Modified

### `src/classification/classifier.cpp`
- Added index range check to threshold condition in `classify_and_format()`
- Before: `if (conf < threshold)`
- After: `if (conf < threshold || idx < 0 || idx >= 1000)`
- Prevents incorrect classification results from out-of-range index

### `src/common/toolbox.cpp`
- Applied `std::max` to resize size calculation in `center_crop()`
- Before: `int new_w = int(img.cols * scale), new_h = int(img.rows * scale)`
- After: `new_w = std::max(target_w, ...)`, `new_h = std::max(target_h, ...)`
- Prevents off-by-one resize caused by floating point rounding

## Added

### `src/common/INFER_ARCHITECTURE.md`
- Documented inference architecture based on HailoRT VStream API
- Described HailoInfer class constructor and public interface
- Added explanation and diagram for write/read thread separation
- Included data flow summary, usage example, and VStream implementation characteristics

### `analyze_logs.py`
- Added log analysis script

### `logs/2026-05-09_212316.csv`, `logs/analysis.png`, `logs/analysis_212316.png`
- Added log data and analysis result images

## Deleted

### `logs/2026-05-09_*.csv` (13 files)
- Removed old log files

### `src/classification/README.md`
- Removed classification directory README

### `src/classification/classifier.jpg`
- Removed test image
